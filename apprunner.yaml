version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: latest
  command: gunicorn app:app
  network:
    port: 8000
    env: PORT
  env:
    - name: FLASK_APP
      value: "app.py"